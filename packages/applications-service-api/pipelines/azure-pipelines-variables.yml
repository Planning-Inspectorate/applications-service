variables:
  # VERSION INFO ----------------------------------------------------------
  version.MajorMinor: '1.0' # Manually adjust the version number as needed for semantic versioning. Revision is auto-incremented.
  version.Revision: $[counter(variables['version.MajorMinor'], 0)]
  versionNumber: $(version.MajorMinor).$(version.Revision)
  # -----------------------------------------------------------------------

  dockerFilePath: $(Build.SourcesDirectory)/packages/applications-service-api/Dockerfile
  repository: applications-service/applications-service-api
  webAppName: pins-app-applications-service-applications-api-$(ENVIRONMENT)-$(REGION_SHORT)-001
