{% macro textHeadingSummary(heading, summary) %}
  {% if heading or summary %}
    {% if heading %}
      <h2 class="govuk-heading-m">
        {{ heading }}
      </h2>
    {% endif %}

    {% if summary %}
      <p>
        {{ summary }}
      </p>
    {% endif %}
  {% endif %}
{% endmacro %}
