{% macro uiTagLink(tag) %}
  <a
    alt="{{ tag.alt }}"
    class="ui-tag-link"
    data-cy="ui-tag-link"
    href="{{ tag.link }}"
  >
    {% if tag.icon == 'close' %}
      <span class="ui-tag-link__close-icon"></span>
    {% endif %}

    <span class="ui-tag-link__text">{{ tag.text }}</span>
  </a>
{% endmacro %}
