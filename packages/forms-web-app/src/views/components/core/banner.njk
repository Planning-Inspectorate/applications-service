{% from "govuk/components/phase-banner/macro.njk" import govukPhaseBanner %}

{% set linkHtml = '<a class="govuk-link" data-cy="Feedback" href="' + serviceFeedbackUrl + '">' + t("common:betaBanner.feedback") + '</a>' %}

{{ govukPhaseBanner({
  tag: {
    text: "Beta"
  },
  html: t("common:betaBanner.content", { link: linkHtml })
}) }}
