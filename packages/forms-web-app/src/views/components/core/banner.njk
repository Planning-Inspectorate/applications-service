{% from "govuk/components/phase-banner/macro.njk" import govukPhaseBanner %}

{% set link = '<a class="govuk-link" data-cy="Feedback" href="' + serviceFeedbackUrl + '">' + t("common.feedback") + '</a>' %}

{{ govukPhaseBanner({
  tag: {
    text: t("global.banner.tagText")
  },
  html: t("global.banner.html", { link: link })
}) }}
