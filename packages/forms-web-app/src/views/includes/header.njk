{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/skip-link/macro.njk" import govukSkipLink %}

{% set logo = "pins-logo.njk" %}
{% if language == 'cy' %}
	{% set logo = "pins-logo-cy.njk"%}
{% endif %}

<header role="banner">
	{{ govukSkipLink({
		href: '#main-content',
		text: 'Skip to main content'
	}) }}

	<div class="pins-header">
		<div class="govuk-header__container pins-header__container govuk-width-container">
			<div class="govuk-header__logo">
				<span class="govuk-header__logotype">
					{% include "components/core/" + logo %}
				</span>
			</div>
			<div class="govuk-header__content">
				<span class="govuk-header__service-name">{{ global.headerTitle }}</span>
			</div>
		</div>
	</div>

	<div class="govuk-width-container">
		{% include "components/core/banner.njk" %}

		{% if featureFlag.allowPrimaryNavigation %}
			{% include "includes/primary-navigation.njk" %}
		{% endif %}

		{% if backLinkUrl %}
			{{ govukBackLink({
				attributes: {
					'data-cy': 'back',
					'id': 'back-link'
				},
				href: backLinkUrl,
				text: global.backLinkText
			}) }}
		{% endif %}
	</div>
</header>