{% from "components/ui/checkbox-accordion.njk" import uiCheckboxAccordion with context %}

<form method="post" class="filter-sidebar__form">
	{{ uiCheckboxAccordion(
		true,
		filters,
		t('common.filter'),
		mapConfig.totalProjects + " " + t('projectsMap.common.projects'),
		"project-map-page-filters",
		'modal-on-tablet'
	) }}
</form>
