<div>
    <span class="govuk-caption-l">{{ projectName }}</span>

    <h1 class="govuk-heading-xl">{{ pageHeading }}</h1>

    <h2 class="govuk-heading-m"> About the project</h2>
    <p class="govuk-body">Type of application: <b>{{proposal}}</b></p>
    <p class="govuk-body">{{ summary }}</p>

    {% if webAddress %}
        <a class="govuk-link" href="{{webAddress}}">View the developer’s website</a>
    {% endif %}
</div>