{% from "components/ui/results-per-page.njk" import uiResultsPerPage %}

<p>
  Showing <strong>{{ paginationData.fromRange }}</strong> to <strong>{{ paginationData.toRange }}</strong> of <strong>{{ paginationData.totalItems }}</strong> representations, newest first.
</p>

{{ uiResultsPerPage(resultsPerPage) }}