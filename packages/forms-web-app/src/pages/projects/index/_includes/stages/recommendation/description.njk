{% if recommendationCompletedDate %}
    {% set recommendationText %}
        The date set for this to be completed is {{ recommendationCompletedDate | date }}
    {% endset %}
{% endif %}

<p class="govuk-body">
	{{ recommendationText }}
</p>