{% set summaryHtml %}
<div class="summary-content">
  <span>{{ processGuideStages.decision.title }}</span>
  {{ stageProgressTag( applicationData.status.number, 6, t) }}
</div>
{% endset %}

{{ govukDetails({
	summaryHtml: summaryHtml,
	html: processGuideStages.decision.html,
	open: applicationData.status.text === projectStages[6],
	classes: 'pins-modded-summary-full-width'
}) }}

<hr class="govuk-section-break govuk-section-break--visible">