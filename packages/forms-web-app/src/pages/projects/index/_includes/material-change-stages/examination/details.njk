{% set summaryHtml %}
<div class="summary-content">
  <span>{{ processGuideStages.examination.title }}</span>
  {{ stageProgressTag( applicationData.status.number, 4, t) }}
</div>
{% endset %}

{{ govukDetails({
	summaryHtml: summaryHtml,
	html: processGuideStages.examination.html,
	open: applicationData.status.text === projectStages[4],
	classes: 'pins-modded-summary-full-width'
}) }}

<hr class="govuk-section-break govuk-section-break--visible">