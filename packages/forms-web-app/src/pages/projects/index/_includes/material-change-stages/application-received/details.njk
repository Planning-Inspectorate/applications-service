{% set summaryHtml %}
<div class="summary-content">
  <span>{{ processGuideStages.applicationReceived.title }}</span>
  {{ stageProgressTag( applicationData.status.number, 2, t) }}
</div>
{% endset %}

{{ govukDetails({
	summaryHtml: summaryHtml,
	html: processGuideStages.applicationReceived.html,
	open: applicationData.status.text === projectStages[2],
	classes: 'pins-modded-summary-full-width'
}) }}

<hr class="govuk-section-break govuk-section-break--visible">