const { section51IndexRoute } = require('./config');

describe('pages/projects/section-51/index/config', () => {
	describe('#section51IndexRoute', () => {
		it('should return the section 51 index route', () => {
			expect(section51IndexRoute).toEqual('');
		});
	});
});
