{% from "components/ui/step-by-step-navigation.njk" import stepByStepNavigation %}

{% set params =
  {
    steps: [
      {
          heading: {
            text: t('processGuide.preApplication.subHeading')
          },
          content: {
            text: [
              t('processGuide.preApplication.paragraph1'),
              t('processGuide.preApplication.paragraph2')
            ]
          },
          subList: [
            {
              text: t('processGuide.preApplication.linkText'),
              href: processGuide.steps.preApplication.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.acceptance.subHeading')
          },
          content: {
            text: t('processGuide.acceptance.paragraph')
          },
          subList: [
            {
              text: t('processGuide.acceptance.linkText'),
              href: processGuide.steps.acceptance.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.preExamination.subHeading')
          },
          content: {
            text: [
              t('processGuide.preExamination.paragraph1'),
              t('processGuide.preExamination.paragraph2'),
              t('processGuide.preExamination.paragraph3')
            ]
          },
          subList: [
            {
              text: t('processGuide.preExamination.linkText'),
              href: processGuide.steps.preExamination.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.examination.subHeading')
          },
          content: {
            text: t('processGuide.examination.paragraph')
          },
          subList: [
            {
              text: t('processGuide.examination.linkText'),
              href: processGuide.steps.examination.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.recommendation.subHeading')
          },
          content: {
            text: t('processGuide.recommendation.paragraph1')
          },
          subList: [
            {
              text: t('processGuide.recommendation.linkText'),
              href: processGuide.steps.recommendation.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.decision.subHeading')
          },
          content: {
            text: t('processGuide.decision.paragraph')
          },
          subList: [
            {
              text: t('processGuide.decision.linkText'),
              href: processGuide.steps.decision.url
            }
          ]
      },
      {
          heading: {
            text: t('processGuide.postDecision.subHeading')
          },
          content: {
            text: t('processGuide.postDecision.paragraph')
          },
          subList: [
            {
              text: t('processGuide.postDecision.linkText'),
              href: processGuide.steps.postDecision.url
            }
          ]
      }
    ]
  }
%}

{% if templateID !== 'main' %}
  {% set params = {
    pretitle: "Part of",
    title: processGuide.steps.processGuide.title,
    url: processGuide.steps.processGuide.url,
    steps: params.steps
  } %}
{% endif %}

{% set state = 'static' %}

{% if templateID === 'main' %}
  {% set state = 'dynamic' %}
{% endif %}

{{ stepByStepNavigation(params, processGuide.activeStep.step, templateID, state, t) }}