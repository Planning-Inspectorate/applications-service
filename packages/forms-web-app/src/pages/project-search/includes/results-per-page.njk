{% from "components/ui/results-per-page.njk" import uiResultsPerPage %}

<p>
  Showing <strong>{{ pagination.paginationData.fromRange }}</strong> to <strong>{{ pagination.paginationData.toRange }}</strong> of <strong>{{ pagination.paginationData.totalItems }}</strong> projects.
</p>

{{ uiResultsPerPage(resultsPerPage) }}