{% from "components/ui/checkbox-accordion.njk" import uiCheckboxAccordion %}
{% from "components/layout/slots.njk" import layoutSlots %}
{% from "components/ui/vertical-tabs.njk" import uiVerticalTabs %}

{{ layoutSlots([
  uiCheckboxAccordion(
    true,
    filters,
    "Filter",
    pagination.paginationData.totalItems + " projects",
    "project-search-page-filters",
    'modal-on-tablet'
  ),
  uiVerticalTabs(
    t("pages.projectSearch.relatedContent.heading"),
    null,
    relatedContentLinks,
    t("pages.projectSearch.relatedContent.label"),
    "h3"
  )
]) }}