{% from "components/ui/checkbox-accordion.njk" import uiCheckboxAccordion with context %}
{% from "components/layout/slots.njk" import layoutSlots %}
{% from "components/ui/vertical-tabs.njk" import uiVerticalTabs %}

{{ layoutSlots([
  uiCheckboxAccordion(
    true,
    filters,
    t('common.filter'),
    pagination.paginationData.totalItems + " " + t('projectSearch.common.projects'),
    "project-search-page-filters",
    'modal-on-tablet'
  ),
  uiVerticalTabs(
    t('projectSearch.sidebar.relatedContent'),
    null,
    relatedContentLinks,
    "Related content navigation",
    "h3"
  )
]) }}