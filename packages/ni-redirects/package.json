{
	"name": "@pins/ni-redirects",
	"version": "0.0.0",
	"private": true,
	"engines": {
		"node": ">=22.0.0 <23.0.0"
	},
	"type": "module",
	"scripts": {
		"deploy:prod": "wrangler deploy -e prod",
		"deploy:test": "wrangler deploy -e test",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "npx jest",
		"test:cov": "npx jest --coverage --verbose"
	},
	"devDependencies": {
		"cross-env": "*",
		"jest": "*",
		"wrangler": "*"
	}
}
