{
	"name": "@pins/ni-redirects",
	"version": "0.0.0",
	"private": true,
	"engines": {
		"node": ">=18.0.0 <19.0.0"
	},
	"type": "module",
	"scripts": {
		"deploy:prod": "wrangler deploy -e prod",
		"deploy:test": "wrangler deploy -e test",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest",
		"test:cov": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --coverage --verbose"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"jest": "^29.7.0",
		"wrangler": "^3.0.0"
	}
}
