{
	"name": "@pins/ni-redirects",
	"version": "0.0.0",
	"private": true,
	"engines": {
		"node": ">=22.0.0 <23.0.0"
	},
	"type": "module",
	"scripts": {
		"deploy:prod": "wrangler deploy -e prod",
		"deploy:test": "wrangler deploy -e test",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "NODE_OPTIONS=--experimental-vm-modules npx jest",
		"test:cov": "NODE_OPTIONS=--experimental-vm-modules npx jest --coverage --verbose"
	},
	"devDependencies": {
		"cross-env": "*",
		"jest": "*",
		"wrangler": "*"
	}
}
